# CLAUDE.md

このファイルは、このリポジトリでClaude Code(AIアシスタント)が作業する際に読む、プロジェクトの背景・設計方針・注意点のまとめです。コードを読めば分かることは書かず、「なぜそうなっているか」「何を却下したか」「何に気をつけるべきか」を中心に書いています。

詳しい時系列の経緯(検討過程・却下案とその理由を含む完全版)は、チームのNotion/Discordにある開発ログを参照してください。

## プロジェクト概要

**置き換えダイエット支援アプリ**。Progateプロプラン限定ハッカソン(2026年9月、3週間、3人チーム)の成果物。

- 想定ユーザー: 美味しいものを食べたいがダイエットしたい人
- 課題: ダイエット中「これが食べたい」と思ったとき、我慢するか諦めて食べるかの二択しかなく、何に置き換えればいいか分からない
- コア体験: 料理名を入力 → AIが「置き換えレシピ」を提案 → 実際に食べたかどうかを記録 → 削減カロリーが週単位で積み上がる → 目標体重までの進捗が見える

### 機能の優先順位(MVP設計、決定理由込み)

| 優先度 | 機能 | 状態 |
|---|---|---|
| 1位 | 料理名→AIが置き換えレシピ案を提示 | 完成 |
| 2位 | 実食カロリー記録、差分計算・週単位積算 | 完成 |
| 3位 | 目標体重・期日から1日の削減カロリー目安を算出 | 完成 |
| 4位 | 体重管理(記録・グラフ表示) | 完成 |
| 5位 | 体重推移予測(線形回帰+移動平均+停滞期検出) | **未着手**(設計方針は決定済みだが、コードとしては未実装。コードベースを検索しても該当ロジックが存在しないことを確認済み) |
| 6位 | 食事写真からのカロリー計算 | **見送り**(画像認識の難易度が高いため最初から対象外) |

1〜3位は「置き換え提案→効果の記録→目標への進捗」という1本の軸でつながる核であり、3週間で確実に完成させる部分。4位は余力があれば着手する位置づけで、実際に完成した。5位も同じ位置づけだったが、設計の議論(線形回帰+移動平均+停滞期検出の3点セット採用)まで進んだところで止まっており、実装には至っていない。

深夜ラジオ機能(後述)は、この優先順位の外側にある「わくわく」を狙った追加機能。

## 技術スタック(と選定理由)

- **バックエンド**: Python + Flask(gunicorn運用)。元々は機能ごとにFastAPI/Flaskが混在していたが、メンターの助言もありFlaskに統一した。
- **DB / 認証 / ファイルストレージ**: Supabase(Postgres + Auth + Storage が1つでまかなえる、無料枠で足りる)。
  - 自前サーバー(AWS/Azure/さくらのVPS)は「常時課金・設定ミスで高額請求リスク」を理由に不採用。
  - RailwayはNMLの無料試用クレジット制の可能性が高く不採用。
- **AI**: OpenAI API(レシピ提案・深夜ラジオの台本生成)。利用上限は$10(auto-reload OFF)で運用し、想定外の高額課金を構造的に防いでいる。
- **音声合成(深夜ラジオ機能)**: VOICEVOX(無料、キャラクター性のある日本語音声)。Google Cloud TTS(カード登録必須)・Gemini TTS(無料枠なし)は却下。
- **ホスティング**: Render(無料枠)。メインアプリとVOICEVOXエンジンは**別サービス**としてデプロイしている(理由: 別ソフトウェア、音声合成のCPU負荷を分離するため)。
- **フロントエンド**: 素のHTML/CSS/JS(`static/`配下)。Reactは「変更規模が大きい割に見た目の改善に直結しない」として導入を見送った。

## アーキテクチャ

`app.py`が全ルートを持つ単一のFlaskアプリ(旧・機能ごとに分かれていた複数アプリを統合済み)。各機能はサービス層・リポジトリ層に分離されている。

```
app.py                     ルーティング全体、画面(static/*.html)配信
recipe_service.py          機能1: AIによる置き換えレシピ提案
meal_logs_repository.py    機能2: 食事ログのCRUD
goal_repository.py         機能3: 目標体重・削減カロリー計算
target_calculation.py      機能3: 計算ロジック(AIを使わない純粋な算術)
weight_repository.py       機能4: 体重記録のCRUD
latest_suggestions.py      機能1→2の橋渡し(直近の提案をプロセス内メモリで保持)
auth_service.py            Supabase Authのラッパー(サインアップ/ログイン)

radio_service.py           深夜ラジオ機能のオーケストレーション
radio_prompt.py            台本生成プロンプトの組み立て
voicevox_client.py         VOICEVOXエンジンとの通信(音声合成)
radio_episode_repository.py  エピソード(台本・音声)のCRUD、Storage連携
radio_memory_repository.py   長期記憶(milestone/highlight/reflection)のCRUD
```

「機能ごとに担当者が分かれる」という開発体制を反映し、ファイルも機能単位で分かれている。

### Shared Database パターン

機能間の連携(例: 機能1の提案→機能2の記録、機能4の体重→機能3の目標計算)は、HTTP通信ではなくDB(またはプロセス内メモリ)経由の「Shared Database」パターンで行っている。ブラウザ側のJS変数で受け渡す案は、「提案を見たタイミングと記録するタイミングがズレる実際の使われ方」に対応できないため却下した。

### 「1人が複数回記録するデータ」の主キー設計

`weight_logs`・`meal_logs`など、1ユーザーが何度も行を追加するテーブルは、`user_id`ではなく独立した連番`id`を主キーにしている(`user_id`を主キーにすると1人1行しか持てなくなるため)。逆に`goals`は当初この設計ミス([id]カラムが操作ミスで消失した事故)から`user_id`主キー(1人1行、上書き)になっている — 履歴管理が必要になったら見直しが必要な既知の非対称。

## 深夜ラジオ機能(実験的機能)について

その日の食事ログ・体重・目標進捗・本人が入力した「今日、DJたちに教えたいこと」をもとに、AIが台本を書き、VOICEVOXで2人のDJ(第三者視点で本人の噂話をする構図)が読み上げる音声を生成する機能。

- **生成の入口は`/record`(食事ログ記録画面)のチェックボックスのみ**。`/radio`画面自体には生成ボタンはなく、状態(未生成/生成中/再生可能/再生済み)を表示するだけの画面になっている。
- 生成は裏スレッドで非同期に行われる(食事ログ記録のリクエスト自体はすぐ応答が返る)。フロントは`/radio/status`を5秒おきにポーリングして完了を検知する。
- 長期記憶(Generative Agents方式): `radio_memories`テーブルに`milestone`(コード判定、削除しない)・`highlight`(AI判定、直近5件保持)・`reflection`(10回に1回の傾向要約、直近5件保持)の3種類を記録し、重要度と経過日数からスコアリングして上位数件だけを毎回のプロンプトに渡す。「1つの要約を上書きし続ける」方式(story_memory案)は伝言ゲーム的に内容が歪むリスクがあるため却下し、この追記型の設計にした。
- 共有機能はオプトイン(`is_shared`)。他人の情報を無断で見られる設計は個人情報保護の観点で最初から却下している。
- 保持件数は本人ごとに直近2件(`MAX_EPISODES_PER_USER`)、アプリ全体で80件(`MAX_TOTAL_EPISODES`)までの安全弁がある。

## 既知の制約・ハマりどころ

### VOICEVOXの無料枠はCPU 0.1個分しかない

Render無料インスタンスのVOICEVOXは、実質CPUの1/10しか割り当てられていない。これにより:

- 休止状態からの起動に**90秒以上**かかることがある(公式の「50秒以上」という目安を超えることがある、実測済み)
- 起動途中は`/version`等への応答が「502 Bad Gateway」になることがあり、これを「応答が来た=起動完了」と誤判定しないよう、`wake_up()`は`raise_for_status()`で200を確認するまでリトライする設計になっている
- CPUが遅すぎて、起動が完了した後の音声合成そのものが`The read operation timed out`で失敗することがある。これは起動待ちの問題ではなく、無料枠のCPU性能そのものの限界であり、コード側のリトライだけでは根本解決できない
- 対策として、`wake_up()`(裏スレッドから呼ばれる場合はデフォルトの余裕がある値、同期呼び出しの場合は短いパラメータを明示指定)と`synthesize_line()`のリトライ回数は、それぞれ「粘りすぎるとメモリを圧迫する」「短すぎると失敗しやすい」のバランスを取った値になっている。安易に数字だけ大きくしないこと
- 発表・デモ本番では、事前に一度自分でラジオを生成しておく(ライブ生成に賭けない)のが最も確実な回避策

### Supabaseクライアントは使い回すこと(メモリリーク対策済み)

各repositoryファイルの`get_client()`は、モジュールレベルでクライアントをキャッシュして使い回す設計になっている(`threading.Lock()`でダブルチェックロッキング)。**毎回`create_client()`し直すと、5秒おきにポーリングされる`/radio/status`のようなエンドポイントで新しい接続が作られ続け、本番で実際にメモリ超過(512MB)による自動再起動が70〜100分おきに繰り返される原因になった**。新しいrepositoryを追加するときは、既存の`get_client()`の実装パターン(引数なし呼び出し時はキャッシュを使い、明示的にurl/keyを渡した場合のみ新規作成)を踏襲すること。

### ただし`sign_in`/`sign_up`だけは共有クライアントを使わないこと

`client.auth.sign_in_with_password()`や`client.auth.sign_up()`は、そのクライアントインスタンスの内部状態に「今ログイン中のユーザー」の認証情報を書き込む。これを上記の共有クライアントで行うと、**複数ユーザーが同時にログインした場合、後からログインした人の権限で他のユーザーのDB操作が実行されてしまう**深刻な設計ミスになる。`auth_service.py`の`sign_up`/`sign_in`は、そのため専用の使い捨てクライアント(`_fresh_client()`)を使っている。同じパターンで新しい認証系の処理を書くときは、このクライアント分離を必ず踏襲すること。

### Procfileだけでは不十分

`Procfile`はHeroku/Railway等が自動で読むファイルで、**Renderは読まない**。Renderにデプロイする場合は、ダッシュボードのStart Command欄(または`render.yaml`)に同じ起動コマンドを直接設定する必要がある。

- `-w`(ワーカー数)は必ず`1`にすること。ラジオ生成の「今生成中か」やレシピ提案の「直近の提案」をプロセス内メモリ(DBではない)で持っているため、複数ワーカーにすると別プロセスに振り分けられたリクエストからはその状態が見えなくなる。
- 複数人からの同時アクセスには、ワーカー数ではなく`--threads`で対応する(`gunicorn app:app -w 1 --threads 2 --timeout 300`)。
- `--timeout`はデフォルト30秒では足りない処理があるため300秒に延長している。

### RLSは意図的に無効化している

`meal_logs`・`goals`・`weight_logs`はRow Level Securityを無効化している(ポリシー設定がハッカソンの短期間には複雑すぎるため)。`radio_episodes`だけRLSが有効な状態で作られてしまったが、サーバー側は常にservice role keyでアクセスするため動作上の一貫性の問題にはなっていない。本番運用を続けるならRLS導入は検討課題として残っている。

## 開発時の運用ルール

### Gitブランチ運用

- `main`に直接コミットしない。`feature/xxx`・`fix/xxx`ブランチを作り、PRを経由してマージする。
- **一度マージされたブランチに追加コミットしても、mainには反映されない**(コミットが「浮く」)。新しい作業は必ず最新の`main`から新しいブランチを切ること。作業中に「あれ、このブランチもうマージされてる?」と思ったら、`git log --oneline` と GitHub 上のPR一覧を確認する。

### PRテンプレートのルール

`.github/PULL_REQUEST_TEMPLATE.md`にProgate運営から指定された学習フロー用のルールがある。**AIは「登場する概念」を名前と場所(ファイル・行番号)だけ書き、その説明は書かない**。説明は人間が自分の言葉でPRコメントに書く(学習機会を確保するため)。ユーザーから明示的に「説明も書いて」と頼まれた場合のみ書くが、その場合も本来の運用ルールとは異なる例外対応であることを伝える。

### コミットメッセージ

AIが生成・編集したコミットには`Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`を付与する(AI支援を受けた事実を正直に記録する方針)。

### 「今困っていないことのために部品を増やさない」

このプロジェクト全体を貫く判断基準。認証・非同期ジョブ・定期バッチ・データ分析基盤・RAG(embeddings)などは、実際に必要になるまで導入しない。深夜ラジオの長期記憶機能でRAGを検討したときも「データ量が少なすぎて全件をそのままプロンプトに渡せば足りる」と判断し導入しなかった。新しい依存関係や抽象化を追加する前に、「今、具体的に何が困っているか」を確認すること。

### コードは基本的にユーザー(チームメンバー)が書く

AIがコードを代わりに書きすぎると学習機会を奪ってしまう、という指摘を過去に受けている。特にGitのコンフリクト解消のような「手を動かして学ぶべき」作業は、頼まれない限り代行せず、手順の説明に留めること。一方で、大きな実装(複数ファイルにまたがる機能追加など)をまとめて依頼された場合はその限りではない。

## チーム体制(2026-09-15時点)

- 伊佐さん: アレルギー機能の実装、記録ログのキャンセル設定
- くわばらさん: 記録ログのキャンセル設定(伊佐さんと協力)、デザイン全般(主担当)
- 自分(ユーザー): エッジケース・異常系対応

## セッション開始時のルール

新しいセッションを開始するときは、必ず最初に `git pull` を実行してから作業を始めること。
これにより、他のメンバーが更新した最新の情報を踏まえた状態で作業を開始できるようにする。

## CLAUDE.mdの更新ルール

作業中に、技術選定・仕様・設計方針についての重要な決定があった場合は、
ユーザーから明示的に指示されなくても、その都度CLAUDE.mdを自発的に更新すること。
