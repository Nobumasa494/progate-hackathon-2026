
# .env.example → コピーして .env にリネームし、実値を入れること
OPENAI_API_KEY=
SUPABASE_URL=
SUPABASE_KEY=
FLASK_SECRET_KEY=

# 任意。ローカル開発でデバッグモード(詳細なエラー画面)を使いたい時だけ 1 にする。
# 本番では絶対に設定しないこと(コードが露出する危険がある)。
FLASK_DEBUG=

